<RelativeLayout android:layout_height="match_parent"
    android:layout_width="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:background="@drawable/darin2">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Existing"
        android:id="@+id/existingButton"
        android:textAllCaps="false"
        android:drawableTop="@drawable/ic_find_in_page_black_24dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="173dp"
        />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Gallery"
        android:drawableTop="@drawable/gallery"
        android:id="@+id/galleryButton"
        android:textAllCaps="false"
        android:layout_below="@+id/cameraButton"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Camera"
        android:id="@+id/cameraButton"
        android:textAllCaps="false"
        android:drawableTop="@drawable/ic_camera_black_24dp"
        android:layout_below="@+id/existingButton"
        android:layout_centerHorizontal="true" />


</RelativeLayout>